name: STRR PLATFORM UI CI

permissions:
  contents: read

on:
  pull_request:
    paths:
      - "strr-platform-web/**"
  workflow_dispatch:

defaults:
  run:
    shell: bash
    working-directory: ./strr-platform-web

jobs:
  platform-ui-ci:
    # Match the permissions required by the reusable frontend CI workflow.
    permissions:
      contents: read
      pull-requests: write
    uses: bcgov/bcregistry-sre/.github/workflows/frontend-ci.yaml@main
    with:
      app_name: "strr-platform-ui"
      working_directory: "./strr-platform-web"
      codecov_flag: "strrplatformui"
      node_version: 24
      pnpm_version: 10.0.0
